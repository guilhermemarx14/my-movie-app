<resources>

    <style name="Theme.MyMovieApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnSecondary">@color/secondaryColor</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primaryColor</item>
        <!-- Customize your theme here. -->

    </style>

    <style name="Theme.MyMovieApp.Fragment">
        <item name="android:textColor">@color/lightTextColor</item>
        <item name="android:background">@color/primaryDarkColor</item>
    </style>

    <style name="Theme.MyMovieApp.Toolbar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="android:background">@color/primaryColor</item>
        <item name="drawerArrowStyle">@style/Theme.MyMovieApp.DrawerIcon</item>
    </style>

    <style name="Theme.MyMovieApp.DrawerIcon" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/primaryTextColor</item>
    </style>
</resources>